replicaCount: 1
appVersion: "latest"
image: bitnami/kafka:latest
port:
  broker: 9092
  controller: 9093
nodePort: 30020
clusterId: "4L6g3nShT-eMCtK--X86sw"
rootPassword: kafka-secret
persistence:
  enabled: true
  storageClass: local-storage
  accessModes:
    - ReadWriteOnce
  size: 8Gi
resources:
  requests:
    memory: "1Gi"
    cpu: "500m"
  limits:
    memory: "2Gi"
    cpu: "1"